<p style="color: green"><%= notice %></p>

<%= render partial: "bootstrap/navbar" %>

<h1>Authors</h1>

<div id="authors">
  <% @authors.each do |author| %>
    <p>
      <%= link_to author.name+" "+author.surname, author %> this author has written <%= author.articles_count %> articles
    </p>
  <% end %>
</div>

<%= link_to "New author", new_author_path %>
